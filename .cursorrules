【プロジェクトの基本ルール】
今後のタスクでは、以下の手順を基本ルールとして守ってください。
1. 作業を開始する前に、必ず `npm install` を実行し、依存パッケージがインストール済みの環境を整えてください。
2. コードの修正が完了したら、必要に応じて `npm run lint` や `npm run build` を実行し、動作や構文にエラーがないか検証してから完了報告をしてください。
